<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NowInMoon" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NowInMoon" parent="Base.Theme.NowInMoon" />
</resources>